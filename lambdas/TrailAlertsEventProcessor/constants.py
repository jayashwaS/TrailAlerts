# Severity levels for correlation rules
SEVERITY_LEVELS = {
    "critical": 4,
    "high": 3,
    "medium": 2,
    "low": 1,
    "info": 0
}

# Default time window for correlation in minutes
DEFAULT_WINDOW_MINUTES = 60

# Default minimum severity level for notifications
DEFAULT_MIN_SEVERITY = "medium" 